package protocol

/*
type InternalProtocol interface {
	ID() wrappedProtocol.ID
	Init()
	Start()

	InConnRequested(sourceProto wrappedProtocol.ID, peer peer.Peer) Error
	DialFailed(sourceProto wrappedProtocol.ID, peer peer.Peer, error Error)
	DialSuccess(sourceProto wrappedProtocol.ID, peer peer.Peer) Error
	PeerDown(peer peer.Peer)

	RegisterMessageHandler(msgID message.ID, handler handlers.MessageHandler) Error
	RegisterRequestHandler(requestID request.ID, handler handlers.RequestHandler) Error

	DeliverRequest(request request.Request)
	DeliverMessage(message message.Message)
}
*/
